# NYU Shanghai Library WMS

This is the repository for the frontend of the NYU Shanghai Library Workflow Management System.

## How To Use

To install dependencies and build, run:

```bash
yarn install
yarn run build
```

To start the development server, run:

```bash
yarn run start
```
