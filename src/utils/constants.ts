import { ITag } from '@/utils/interfaces';

// To compose request URL
export const urlPrefix = '/api';

// For tag enumerating and sorting
export const allTags: ITag[] = ['Rush', 'Non-Rush', 'CDL', 'Local', 'NY', 'Reserve', 'DVD', 'ILL', 'Sensitive'];
